/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {
      colors: {
        primary: "#60B5FF",       // Blue
        secondary: "#AFDDFF",     // Light Blue
        background: "#FFECDB",    // Pastel Cream
        accent: "#FFECDB",        // Same as background (optional reuse)
      },
    },
  },
  plugins: [],
}

